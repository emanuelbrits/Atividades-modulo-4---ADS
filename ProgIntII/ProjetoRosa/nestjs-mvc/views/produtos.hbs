<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Produtos</title>
</head>
<body>
  <a href="adicionar">Adicionar produto</a>
  <hr>
  {{#if produtos.length }}
    <table>
      <tr>
        <th>nome</th>
        <th>status</th>
        <th>destinacao</th>
        <th>Taxa de rentabilidade ao ano</th>
        <th>prazo</th>
        <th>Taxa de administração</th>
        <th>Id</th>
      </tr>
    {{#each produtos}}
      <tr>
        <td>{{nome}}</td>
        <td>{{status}}</td>
        <td>{{destinacao}}</td>
        <td>{{TaxaRentabilidadeAA}}</td>
        <td>{{prazo}}</td>
        <td>{{TaxaAdministracao}}</td>
        <td>{{id}}</td>
        <td>
          <a href="produtos/alternar-status?id={{id}}">
            {{#if produto.disponivel}}
              Disponibilizar
            {{else}}
              Suspender
            {{/if}}
          </a>
          <a href="produtos/remover?id={{id}}">Remover</a>
        </td>
      </tr>
    {{/each}}
    </table>
    {{else}}
    <p>Nenhum produto para ser listado</p>
  {{/if}}
</body>
</html>